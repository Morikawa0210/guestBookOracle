<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="guestBook">

	<select id="list" resultType="com.ipsgholdings.guestBook.     DTO">
		select * from product
			where
		product_name like #{product_name}
			order by
		product_code desc
	</select>
	
	<insert id="insert">
	insert into product(product_name,description,price,filename)
	values(#{product_name},#{description},#{price},#{filename})
	</insert>
	
	<select id="detail" resultType="com.ipsgholdings.guestBook.ProductDTO">
		select * from product
		where product_code = #{product_code}
	</select>
	
	<update id="update">
	update product
	set
		product_name=#{product_name},
		price=#{price},
		description=#{description},
		filename=#{filename}
	where product_code=#{product_code}
	</update>
	
	<delete id="delete">
		delete from product
		where product_code=#{product_code}
	</delete>
	
	<select id="filename" resultType="String">
		select filename from product
		where product_code=#{product_code}
	</select>
</mapper>